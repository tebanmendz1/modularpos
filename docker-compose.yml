name: facturapos-cloud

services:
  facturapos-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: facturapos_cloud_app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
